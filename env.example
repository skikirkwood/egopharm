# Contentful Configuration
# Copy this file to .env.local and fill in your actual values

# Your Contentful Space ID
# Find this in Contentful: Settings > General Settings > Space ID
CONTENTFUL_SPACE_ID=your_space_id_here

# Content Delivery API (CDA) Token
# Used by the Next.js app to fetch published content
# Find this in Contentful: Settings > API keys > Content delivery / preview tokens
CONTENTFUL_ACCESS_TOKEN=your_cda_token_here

# Content Preview API (CPA) Token
# Used for live preview to fetch draft content
# Find this in Contentful: Settings > API keys > Content delivery / preview tokens
CONTENTFUL_PREVIEW_ACCESS_TOKEN=your_cpa_token_here

# Content Management API (CMA) Token
# Used by the migration script to create content types
# Generate this in Contentful: Settings > API keys > Content management tokens > Generate personal token
CONTENTFUL_MANAGEMENT_TOKEN=your_cma_token_here

# Contentful Environment (optional, defaults to 'master')
CONTENTFUL_ENVIRONMENT=master

# Preview Secret
# A secret string used to validate preview mode requests
# Generate a random string and set it here (also configure in Contentful)
CONTENTFUL_PREVIEW_SECRET=your_preview_secret_here

# Contentful Personalization (Ninetailed) Configuration
# Get this from app.ninetailed.io or the Contentful Personalization app settings
# Must use NEXT_PUBLIC_ prefix for client-side access
NEXT_PUBLIC_NINETAILED_API_KEY=your_ninetailed_api_key_here

# Ninetailed Environment (main or development)
NEXT_PUBLIC_NINETAILED_ENVIRONMENT=main
